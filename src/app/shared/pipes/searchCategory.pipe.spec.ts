import { SearchCategoryPipe } from './searchCategory';

describe('SearchCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
